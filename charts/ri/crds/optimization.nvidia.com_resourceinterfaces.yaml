---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: resourceinterfaces.optimization.nvidia.com
spec:
  group: optimization.nvidia.com
  names:
    kind: ResourceInterface
    listKind: ResourceInterfaceList
    plural: resourceinterfaces
    shortNames:
    - ri
    singular: resourceinterface
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Target framework kind
      jsonPath: .spec.structureDefinition.rootComponent.kind.kind
      name: Framework
      type: string
    - description: Root component name
      jsonPath: .spec.structureDefinition.rootComponent.name
      name: Root Component
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceInterface
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              optimizationInstructions:
                description: Instructions contains optimization-specific instructions
                  for the workload
                properties:
                  gangScheduling:
                    properties:
                      podGroups:
                        description: PodGroups defines the groups of pods that should
                          be scheduled together
                        items:
                          description: PodGroupDefinition defines a group of pods
                            that should be scheduled together.
                          properties:
                            members:
                              description: Members defines which components belong
                                to this pod group
                              items:
                                description: PodGroupMemberDefinition defines how
                                  to select and filter components for grouping instructions.
                                properties:
                                  componentName:
                                    description: ComponentName references a component
                                      defined in the RI's structureDefinition
                                    type: string
                                  filters:
                                    description: |-
                                      Filters are JQ filter expressions to select specific components (expressions are ANDed)
                                      Example: '(.spec.containers[0].resources.limits["nvidia.com/gpu"] // 0) > 0'
                                      JQ filters are evaluated against individual pod objects, not the root resource spec
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: set
                                  groupByKeyPaths:
                                    description: |-
                                      GroupByKeyPaths are JQ paths to values used for grouping (e.g., owner name, replica key)
                                      If empty, grouping is done via owner reference traversal
                                      Every path must return a single, non-empty value - otherwise grouping will fail
                                      JQ paths are evaluated against individual pod objects, not the root resource spec
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: set
                                required:
                                - componentName
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - componentName
                              x-kubernetes-list-type: map
                            name:
                              description: Name is the unique identifier for this
                                pod group
                              type: string
                          required:
                          - members
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                    required:
                    - podGroups
                    type: object
                type: object
              structureDefinition:
                description: StructureDefinition defines the compute hierarchy and
                  component relationships
                properties:
                  additionalChildKinds:
                    description: |-
                      AdditionalChildKinds lists Kubernetes kinds that are created/managed by this workload
                      but are not explicitly modeled as components (e.g., Deployments, Services).
                      Required for RBAC purposes, etc.
                    items:
                      description: GroupVersionKind represents a Kubernetes API object's
                        group, version, and kind.
                      properties:
                        group:
                          description: Group is the API group of the resource
                          type: string
                        kind:
                          description: Kind is the API kind of the resource
                          type: string
                        version:
                          description: Version is the API version of the resource
                          type: string
                      required:
                      - group
                      - kind
                      - version
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - kind
                    x-kubernetes-list-type: map
                  childComponents:
                    description: ChildComponents defines the child components in the
                      hierarchy
                    items:
                      description: |-
                        ComponentDefinition defines a single component in the workload hierarchy.
                        Components represent logical units of computation that can be optimized independently.
                      properties:
                        instanceIdPath:
                          description: InstanceIdPath is the JQ path to the instance
                            id, for components that hold multiple pod definitions
                            (in array or map)
                          type: string
                        kind:
                          description: Kind specifies the Kubernetes GroupVersionKind
                            for this component
                          properties:
                            group:
                              description: Group is the API group of the resource
                              type: string
                            kind:
                              description: Kind is the API kind of the resource
                              type: string
                            version:
                              description: Version is the API version of the resource
                              type: string
                          required:
                          - group
                          - kind
                          - version
                          type: object
                        name:
                          description: Name is the unique identifier for this component
                            within the RI
                          type: string
                        ownerRef:
                          description: OwnerRef references the parent component in
                            the hierarchy by name (nil for root component)
                          type: string
                        podSelector:
                          description: PodSelector defines how to identify pods belonging
                            to this component
                          properties:
                            componentInstanceSelector:
                              description: |-
                                ComponentInstanceSelector splits a single ComponentDefinition into multiple
                                component instances, each identified by a unique value extracted from the pod.
                                Use this when instances of the same component type represent fundamentally
                                different roles or services (e.g., Dynamo "service" definition produces
                                separate "Frontend", "PrefillWorker", "DecodeWorker" instances).
                                This is distinct from ReplicaSelector: ComponentInstanceSelector creates
                                instances with potentially different specs/behavior, while ReplicaSelector
                                creates replicas of an identical sub-structure.
                              properties:
                                idPath:
                                  description: |-
                                    IdPath is the JQ path to the component instance identifier on the pod
                                    JQ path is evaluated against individual pod objects, not the root resource spec
                                  type: string
                              required:
                              - idPath
                              type: object
                            componentTypeSelector:
                              description: |-
                                ComponentTypeSelector determines whether a pod belongs to this component type
                                by matching a pod label or annotation value via a JQ path.
                                This is the primary mechanism for pod-to-component membership.
                                For example, LWS "leader" uses worker-index="0" to identify leader pods,
                                and LWS "worker" checks for the existence of the leader-name annotation.
                              properties:
                                keyPath:
                                  description: |-
                                    KeyPath is the JQ path to the identifying key/label on the pod
                                    JQ path is evaluated against individual pod objects, not the root resource spec
                                  type: string
                                value:
                                  description: Value is the expected value for the
                                    key (optional - if nil, only key existence is
                                    checked)
                                  type: string
                              required:
                              - keyPath
                              type: object
                            replicaSelector:
                              description: |-
                                ReplicaSelector identifies which replica index or group the pod belongs to.
                                Use this to distinguish between multiple replicas of the same component / component instance
                                sub-structure (e.g., LWS Group 0 vs Group 1, where each group has identical
                                leader/worker descendants). Descendant components automatically inherit the replica
                                context from their ancestor (parent), so ReplicaSelector should only be defined at
                                the level where replicas are created, not repeated in descendants.
                              properties:
                                keyPath:
                                  description: |-
                                    KeyPath is the JQ path to the replica identifier on the pod.
                                    The evaluated result should be the replica index or group identifier (e.g., "0", "1").
                                  type: string
                              required:
                              - keyPath
                              type: object
                          type: object
                        scaleDefinition:
                          description: ScaleDefinition defines how to extract scaling
                            information from this component
                          properties:
                            maxReplicasPath:
                              description: MaxReplicasPath is the JQ path to the maximum
                                replica count
                              type: string
                            minReplicasPath:
                              description: MinReplicasPath is the JQ path to the minimum
                                replica count
                              type: string
                            replicasPath:
                              description: ReplicasPath is the JQ path to the current
                                replica count
                              type: string
                          type: object
                        specDefinition:
                          description: SpecDefinition defines how to extract pod specifications
                            from this component
                          properties:
                            fragmentedPodSpecDefinition:
                              description: |-
                                FragmentedPodSpecDefinition defines how to extract individual pod spec fields
                                when they are scattered across different paths in the component
                              properties:
                                annotationsPath:
                                  description: AnnotationsPath is the JQ path to pod
                                    annotations
                                  type: string
                                containerPath:
                                  description: |-
                                    ContainesPath is the JQ path to a single container specifications
                                    Used when the component has only one container
                                  type: string
                                containersPath:
                                  description: ContainersPath is the JQ path to containers
                                    specifications
                                  type: string
                                imagePath:
                                  description: ImagePath is the JQ path to the container
                                    image
                                  type: string
                                labelsPath:
                                  description: LabelsPath is the JQ path to pod labels
                                  type: string
                                nodeAffinityPath:
                                  description: NodeAffinityPath is the JQ path to
                                    node affinity rules
                                  type: string
                                podAffinityPath:
                                  description: PodAffinityPath is the JQ path to pod
                                    affinity rules
                                  type: string
                                priorityClassNamePath:
                                  description: PriorityClassNamePath is the JQ path
                                    to the priority class name
                                  type: string
                                resourceClaimsPath:
                                  description: ResourceClaimsPath is the JQ path to
                                    DRA resource claims
                                  type: string
                                resourcesPath:
                                  description: ResourcesPath is the JQ path to resource
                                    requirements
                                  type: string
                                schedulerNamePath:
                                  description: SchedulerNamePath is the JQ path to
                                    the scheduler name
                                  type: string
                              type: object
                            metadataPath:
                              description: |-
                                MetadataPath is the JQ path to the component metadata
                                May be used only with PodSpecPath, in cases where pod spec and metadata are separated
                              type: string
                            podSpecPath:
                              description: PodSpecPath is the JQ path to a complete
                                PodSpec object
                              type: string
                            podTemplateSpecPath:
                              description: PodTemplateSpecPath is the JQ path to a
                                complete PodTemplateSpec object
                              type: string
                          type: object
                        statusDefinition:
                          description: |-
                            StatusDefinition defines how to interpret the status of this component
                            should be added only for the root component
                          properties:
                            conditionsDefinition:
                              description: ConditionsDefinition defines how to extract
                                Kubernetes-style conditions
                              properties:
                                messageFieldName:
                                  default: message
                                  description: MessageFieldName is the field name
                                    for the condition text message
                                  type: string
                                path:
                                  description: Path is the JQ path to the conditions
                                    array
                                  type: string
                                reasonFieldName:
                                  default: reason
                                  description: ReasonFieldName is the field name for
                                    the condition reason
                                  type: string
                                statusFieldName:
                                  default: status
                                  description: StatusFieldName is the field name for
                                    the condition status
                                  type: string
                                typeFieldName:
                                  default: type
                                  description: TypeFieldName is the field name for
                                    the condition type
                                  type: string
                              required:
                              - path
                              type: object
                            phaseDefinition:
                              description: PhaseDefinition defines how to extract
                                a simple phase/state string
                              properties:
                                path:
                                  description: Path is the JQ path to the phase/state
                                    field
                                  type: string
                              required:
                              - path
                              type: object
                            statusMappings:
                              description: StatusMappings define how to map extracted
                                status to ResourceStatus values
                              properties:
                                completed:
                                  description: |-
                                    Completed defines matchers for the Completed status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is a JQ expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the JQ expression
                                              to evaluate
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                failed:
                                  description: |-
                                    Failed defines matchers for the Failed status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is a JQ expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the JQ expression
                                              to evaluate
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                initializing:
                                  description: |-
                                    Initializing defines matchers for the Initializing status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is a JQ expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the JQ expression
                                              to evaluate
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                running:
                                  description: |-
                                    Running defines matchers for the Running status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is a JQ expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the JQ expression
                                              to evaluate
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                          required:
                          - statusMappings
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  rootComponent:
                    description: RootComponent defines the top-level component of
                      the workload hierarchy
                    properties:
                      instanceIdPath:
                        description: InstanceIdPath is the JQ path to the instance
                          id, for components that hold multiple pod definitions (in
                          array or map)
                        type: string
                      kind:
                        description: Kind specifies the Kubernetes GroupVersionKind
                          for this component
                        properties:
                          group:
                            description: Group is the API group of the resource
                            type: string
                          kind:
                            description: Kind is the API kind of the resource
                            type: string
                          version:
                            description: Version is the API version of the resource
                            type: string
                        required:
                        - group
                        - kind
                        - version
                        type: object
                      name:
                        description: Name is the unique identifier for this component
                          within the RI
                        type: string
                      ownerRef:
                        description: OwnerRef references the parent component in the
                          hierarchy by name (nil for root component)
                        type: string
                      podSelector:
                        description: PodSelector defines how to identify pods belonging
                          to this component
                        properties:
                          componentInstanceSelector:
                            description: |-
                              ComponentInstanceSelector splits a single ComponentDefinition into multiple
                              component instances, each identified by a unique value extracted from the pod.
                              Use this when instances of the same component type represent fundamentally
                              different roles or services (e.g., Dynamo "service" definition produces
                              separate "Frontend", "PrefillWorker", "DecodeWorker" instances).
                              This is distinct from ReplicaSelector: ComponentInstanceSelector creates
                              instances with potentially different specs/behavior, while ReplicaSelector
                              creates replicas of an identical sub-structure.
                            properties:
                              idPath:
                                description: |-
                                  IdPath is the JQ path to the component instance identifier on the pod
                                  JQ path is evaluated against individual pod objects, not the root resource spec
                                type: string
                            required:
                            - idPath
                            type: object
                          componentTypeSelector:
                            description: |-
                              ComponentTypeSelector determines whether a pod belongs to this component type
                              by matching a pod label or annotation value via a JQ path.
                              This is the primary mechanism for pod-to-component membership.
                              For example, LWS "leader" uses worker-index="0" to identify leader pods,
                              and LWS "worker" checks for the existence of the leader-name annotation.
                            properties:
                              keyPath:
                                description: |-
                                  KeyPath is the JQ path to the identifying key/label on the pod
                                  JQ path is evaluated against individual pod objects, not the root resource spec
                                type: string
                              value:
                                description: Value is the expected value for the key
                                  (optional - if nil, only key existence is checked)
                                type: string
                            required:
                            - keyPath
                            type: object
                          replicaSelector:
                            description: |-
                              ReplicaSelector identifies which replica index or group the pod belongs to.
                              Use this to distinguish between multiple replicas of the same component / component instance
                              sub-structure (e.g., LWS Group 0 vs Group 1, where each group has identical
                              leader/worker descendants). Descendant components automatically inherit the replica
                              context from their ancestor (parent), so ReplicaSelector should only be defined at
                              the level where replicas are created, not repeated in descendants.
                            properties:
                              keyPath:
                                description: |-
                                  KeyPath is the JQ path to the replica identifier on the pod.
                                  The evaluated result should be the replica index or group identifier (e.g., "0", "1").
                                type: string
                            required:
                            - keyPath
                            type: object
                        type: object
                      scaleDefinition:
                        description: ScaleDefinition defines how to extract scaling
                          information from this component
                        properties:
                          maxReplicasPath:
                            description: MaxReplicasPath is the JQ path to the maximum
                              replica count
                            type: string
                          minReplicasPath:
                            description: MinReplicasPath is the JQ path to the minimum
                              replica count
                            type: string
                          replicasPath:
                            description: ReplicasPath is the JQ path to the current
                              replica count
                            type: string
                        type: object
                      specDefinition:
                        description: SpecDefinition defines how to extract pod specifications
                          from this component
                        properties:
                          fragmentedPodSpecDefinition:
                            description: |-
                              FragmentedPodSpecDefinition defines how to extract individual pod spec fields
                              when they are scattered across different paths in the component
                            properties:
                              annotationsPath:
                                description: AnnotationsPath is the JQ path to pod
                                  annotations
                                type: string
                              containerPath:
                                description: |-
                                  ContainesPath is the JQ path to a single container specifications
                                  Used when the component has only one container
                                type: string
                              containersPath:
                                description: ContainersPath is the JQ path to containers
                                  specifications
                                type: string
                              imagePath:
                                description: ImagePath is the JQ path to the container
                                  image
                                type: string
                              labelsPath:
                                description: LabelsPath is the JQ path to pod labels
                                type: string
                              nodeAffinityPath:
                                description: NodeAffinityPath is the JQ path to node
                                  affinity rules
                                type: string
                              podAffinityPath:
                                description: PodAffinityPath is the JQ path to pod
                                  affinity rules
                                type: string
                              priorityClassNamePath:
                                description: PriorityClassNamePath is the JQ path
                                  to the priority class name
                                type: string
                              resourceClaimsPath:
                                description: ResourceClaimsPath is the JQ path to
                                  DRA resource claims
                                type: string
                              resourcesPath:
                                description: ResourcesPath is the JQ path to resource
                                  requirements
                                type: string
                              schedulerNamePath:
                                description: SchedulerNamePath is the JQ path to the
                                  scheduler name
                                type: string
                            type: object
                          metadataPath:
                            description: |-
                              MetadataPath is the JQ path to the component metadata
                              May be used only with PodSpecPath, in cases where pod spec and metadata are separated
                            type: string
                          podSpecPath:
                            description: PodSpecPath is the JQ path to a complete
                              PodSpec object
                            type: string
                          podTemplateSpecPath:
                            description: PodTemplateSpecPath is the JQ path to a complete
                              PodTemplateSpec object
                            type: string
                        type: object
                      statusDefinition:
                        description: |-
                          StatusDefinition defines how to interpret the status of this component
                          should be added only for the root component
                        properties:
                          conditionsDefinition:
                            description: ConditionsDefinition defines how to extract
                              Kubernetes-style conditions
                            properties:
                              messageFieldName:
                                default: message
                                description: MessageFieldName is the field name for
                                  the condition text message
                                type: string
                              path:
                                description: Path is the JQ path to the conditions
                                  array
                                type: string
                              reasonFieldName:
                                default: reason
                                description: ReasonFieldName is the field name for
                                  the condition reason
                                type: string
                              statusFieldName:
                                default: status
                                description: StatusFieldName is the field name for
                                  the condition status
                                type: string
                              typeFieldName:
                                default: type
                                description: TypeFieldName is the field name for the
                                  condition type
                                type: string
                            required:
                            - path
                            type: object
                          phaseDefinition:
                            description: PhaseDefinition defines how to extract a
                              simple phase/state string
                            properties:
                              path:
                                description: Path is the JQ path to the phase/state
                                  field
                                type: string
                            required:
                            - path
                            type: object
                          statusMappings:
                            description: StatusMappings define how to map extracted
                              status to ResourceStatus values
                            properties:
                              completed:
                                description: |-
                                  Completed defines matchers for the Completed status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is a JQ expression
                                        that matches against the object for status
                                        matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the JQ expression
                                            to evaluate
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              failed:
                                description: |-
                                  Failed defines matchers for the Failed status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is a JQ expression
                                        that matches against the object for status
                                        matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the JQ expression
                                            to evaluate
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              initializing:
                                description: |-
                                  Initializing defines matchers for the Initializing status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is a JQ expression
                                        that matches against the object for status
                                        matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the JQ expression
                                            to evaluate
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              running:
                                description: |-
                                  Running defines matchers for the Running status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is a JQ expression
                                        that matches against the object for status
                                        matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the JQ expression
                                            to evaluate
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                            type: object
                        required:
                        - statusMappings
                        type: object
                    required:
                    - name
                    type: object
                required:
                - rootComponent
                type: object
            required:
            - structureDefinition
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
