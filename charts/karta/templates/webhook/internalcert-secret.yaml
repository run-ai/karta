{{- if and .Values.operator.enabled .Values.webhook.enabled (eq .Values.webhook.cert.source "selfSigned") -}}
# Empty placeholder; the rotator fills it in place. No data here so upgrades never clobber a live cert.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "karta.webhook.secretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "karta.labels" . | nindent 4 }}
{{- end -}}
