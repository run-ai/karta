apiVersion: kai-bolt.runai.ai/v1alpha1
kind: ResourceInterpretationDefinition
metadata:
  name: raycluster
  namespace: kai-bolt-system
  labels:
    workload.type: distributed-computing
    ai.framework: ray
    example.type: functional
spec:
  structureDefinition:
    rootComponent:
      name: "raycluster"
      # specPath omitted for root component (defaults to .spec)
      kind:
        group: "ray.io"
        version: "v1"
        kind: "RayCluster"
      statusDefinition:
        conditionsDefinition:
          path: ".status.conditions"
          typeFieldName: "type"
          statusFieldName: "status"
        statusMappings:
          running:
            byConditions:
            - type: "Ready"
              status: "True"
          failed:
            byConditions:
            - type: "Ready"
              status: "False"

    childComponents:
    # Head node - Ray cluster coordinator and scheduler
    - name: "head"
      specPath: ".spec.headGroupSpec"
      kind:
        group: "apps"
        version: "v1"
        kind: "Deployment"
      ownerName: "raycluster"
      childSpecDefinition:
        podTemplateSpecPath: ".spec.headGroupSpec.template"
      statusDefinition:
        conditionsDefinition:
          path: ".status.conditions"
          typeFieldName: "type"
          statusFieldName: "status"
        statusMappings:
          running:
            byConditions:
            - type: "Ready"
              status: "True"
          failed:
            byConditions:
            - type: "Ready"
              status: "False"

    # Worker group - distributed computation nodes
    - name: "worker"
      specPath: ".spec.workerGroupSpecs[]"
      kind:
        group: "apps"
        version: "v1"
        kind: "Deployment"
      ownerName: "raycluster"
      childSpecDefinition:
        podTemplateSpecPath: ".spec.workerGroupSpecs[].template"
      scaleDefinition:
        replicasPath: ".spec.workerGroupSpecs[].replicas"
        minReplicasPath: ".spec.workerGroupSpecs[].minReplicas"
        maxReplicasPath: ".spec.workerGroupSpecs[].maxReplicas"
      statusDefinition:
        conditionsDefinition:
          path: ".status.conditions"
          typeFieldName: "type"
          statusFieldName: "status"
        statusMappings:
          running:
            byConditions:
            - type: "Ready"
              status: "True"
          failed:
            byConditions:
            - type: "Ready"
              status: "False"

  # Optimization instructions
  optimizationInstructions:
    # Gang scheduling for Ray cluster startup coordination
    gangScheduling:
      podGroups:
      - name: "ray-cluster"
        members:
        - componentDefinitionName: "head"
          componentKeyPath: '.metadata.labels["ray.io/cluster"]'
        - componentDefinitionName: "worker"
          componentKeyPath: '.metadata.labels["ray.io/cluster"]'

    # GPU optimization for distributed Ray workloads
    gpuInterconnect:
      acceleratedComponents:
      - componentDefinitionName: "head"
        componentKeyPath: '.metadata.labels["ray.io/cluster"]'
        filter: 
        - 'any(.spec.containers[]; (.resources.limits["nvidia.com/gpu"] // 0) > 0)'
      - componentDefinitionName: "worker"
        componentKeyPath: '.metadata.labels["ray.io/cluster"]'
        filter: 
        - 'any(.spec.containers[]; (.resources.limits["nvidia.com/gpu"] // 0) > 0)'

    # Topology awareness for Ray cluster placement
    topologyAwareness:
      topologyGroups:
      - groupName: "ray-computing"
        preferredPlacement: "zone"
        members:
        - componentDefinitionName: "head"
          componentKeyPath: '.metadata.labels["ray.io/cluster"]'
        - componentDefinitionName: "worker"
          componentKeyPath: '.metadata.labels["ray.io/cluster"]' 