apiVersion: run.ai/v1alpha1
kind: Karta
metadata:
  name: kubeflow-org-xgboostjob-v1
spec:
  structureDefinition:
    rootComponent:
      name: xgboostjob
      kind:
        group: kubeflow.org
        version: v1
        kind: XGBoostJob
      suspendDefinition:
        suspendActions:
          - path: .spec.runPolicy.suspend
            value: "true"
        resumeActions:
          - path: .spec.runPolicy.suspend
            value: "false"
      statusDefinition:
        conditionsDefinition:
          path: .status.conditions
          typeFieldName: type
          statusFieldName: status
          reasonFieldName: reason
          messageFieldName: message
        statusMappings:
          initializing:
          - byConditions:
              - type: Created
                status: "True"
          running:
          - byConditions:
              - type: Running
                status: "True"
          completed:
          - byConditions:
              - type: Succeeded
                status: "True"
          failed:
          - byConditions:
              - type: Failed
                status: "True"
          suspended:
          - byConditions:
              - type: Suspended
                status: "True"
          suspending:
          - byExpression:
              expression: .spec.runPolicy.suspend == true and (.status.replicaStatuses // {} | to_entries | map(.value.active // 0) | add // 0) > 0
              expectedResult: "true"
    childComponents:
      - name: Master
        kind:
          group: ""
          version: v1
          kind: Pod
        ownerRef: xgboostjob
        specDefinition:
          podTemplateSpecPath: .spec.xgbReplicaSpecs.Master.template
        scaleDefinition:
          replicasPath: .spec.xgbReplicaSpecs.Master.replicas // 1
        podSelector:
          componentTypeSelector:
            keyPath: .metadata.labels["training.kubeflow.org/replica-type"]
            value: Master
      - name: Worker
        kind:
          group: ""
          version: v1
          kind: Pod
        ownerRef: xgboostjob
        specDefinition:
          podTemplateSpecPath: .spec.xgbReplicaSpecs.Worker.template
        scaleDefinition:
          replicasPath: .spec.xgbReplicaSpecs.Worker.replicas // 1
        podSelector:
          componentTypeSelector:
            keyPath: .metadata.labels["training.kubeflow.org/replica-type"]
            value: Worker
  optimizationInstructions:
    gangScheduling:
      podGroups:
        - name: job
          members:
            - componentName: Master
              groupByKeyPaths:
                - .metadata.labels["training.kubeflow.org/job-name"]
            - componentName: Worker
              groupByKeyPaths:
                - .metadata.labels["training.kubeflow.org/job-name"]
