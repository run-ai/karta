apiVersion: run.ai/v1alpha1
kind: Karta
metadata:
  name: kubeflow-org-jaxjob-v1
spec:
  structureDefinition:
    rootComponent:
      name: jaxjob
      kind:
        group: kubeflow.org
        version: v1
        kind: JAXJob
      suspendDefinition:
        suspendActions:
          - path: .spec.runPolicy.suspend
            value: "true"
        resumeActions:
          - path: .spec.runPolicy.suspend
            value: "false"
      statusDefinition:
        conditionsDefinition:
          path: .status.conditions
          typeFieldName: type
          statusFieldName: status
          reasonFieldName: reason
          messageFieldName: message
        statusMappings:
          initializing:
          - byConditions:
              - type: Created
                status: "True"
          running:
          - byConditions:
              - type: Running
                status: "True"
          completed:
          - byConditions:
              - type: Succeeded
                status: "True"
          failed:
          - byConditions:
              - type: Failed
                status: "True"
          suspended:
          - byConditions:
              - type: Suspended
                status: "True"
          suspending:
          - byExpression:
              expression: .spec.runPolicy.suspend == true and (.status.replicaStatuses // {} | to_entries | map(.value.active // 0) | add // 0) > 0
              expectedResult: "true"
    childComponents:
      - name: Worker
        kind:
          group: ""
          version: v1
          kind: Pod
        ownerRef: jaxjob
        specDefinition:
          podTemplateSpecPath: .spec.jaxReplicaSpecs.Worker.template
        scaleDefinition:
          replicasPath: .spec.jaxReplicaSpecs.Worker.replicas // 1
        podSelector:
          componentTypeSelector:
            keyPath: .metadata.labels["training.kubeflow.org/replica-type"]
            value: Worker
  optimizationInstructions:
    gangScheduling:
      podGroups:
        - name: job
          members:
            - componentName: Worker
              groupByKeyPaths:
                - .metadata.labels["training.kubeflow.org/job-name"]
