# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 NVIDIA Corporation
#
# A bare Pod that sleeps, so its status.phase settles Running. The Karta library
# reads a Pod straight from status.phase.
apiVersion: v1
kind: Pod
metadata:
  name: karta-e2e-pod
  namespace: default
spec:
  restartPolicy: Never
  automountServiceAccountToken: false
  initContainers:
    - name: init-wait
      image: busybox:1.36
      command: ["sh", "-c", "sleep 8"]
      resources:
        requests: {cpu: 10m, memory: 16Mi}
  containers:
    - name: worker
      image: busybox:1.36
      command: ["sh", "-c", "sleep infinity"]
      resources:
        requests: {cpu: 50m, memory: 32Mi}
