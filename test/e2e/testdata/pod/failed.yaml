# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 NVIDIA Corporation
#
# A bare Pod that runs `sh -c "sleep 3; exit 1"` and exits non-zero, so status.phase
# settles Failed. The Karta library reads it as Failed. A distinct name avoids
# colliding with the other Pod flows.
apiVersion: v1
kind: Pod
metadata:
  name: karta-e2e-pod-failed
  namespace: default
spec:
  restartPolicy: Never
  automountServiceAccountToken: false
  initContainers:
    - name: init-wait
      image: busybox:1.36
      command: ["sh", "-c", "sleep 8"]
      resources:
        requests: {cpu: 10m, memory: 16Mi}
  containers:
    - name: worker
      image: busybox:1.36
      command: ["sh", "-c", "sleep 10; exit 1"]
      resources:
        requests: {cpu: 50m, memory: 32Mi}
